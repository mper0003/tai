data:
  configManagementPlugins: |
    - name: vela
      init:
        command: ["vela", "traits"]
      generate:
        command: ["sh"]
        args:
          - -c
          - |
            vela dry-run
# run the following command to patch the argo-cm configmap
# kubectl -n argocd patch cm/argocd-cm -p "$(cat vela-test/vela/argo-cm-patch.yaml)"
